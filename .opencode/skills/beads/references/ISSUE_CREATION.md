# Issue Creation

Create durable work with enough context, acceptance criteria, and priority for another session to act without reconstructing intent. Select the issue type that describes the work, not the current activity.

Use `bd create --help` for current flags and `--json` when structured output improves reliability. Search existing issues before creating a duplicate.
